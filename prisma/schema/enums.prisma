enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum status {
    ACTIVE
    INACTIVE
    SUSPENDED
    BANNED
}

enum PropertyStatus {
    AVAILABLE
    UNAVAILABLE
    PENDING
    RENTED
}

enum FacingDirection {
    NORTH
    SOUTH
    EAST
    WEST
}

enum SizeUnit {
    SQFT
    SQM
}

enum PreferredTenant {
    FAMILY
    BACHELOR
    STUDENT
    JOB_HOLDER
    OFFICE_COMMERCIAL
    APARTMENT
    ANY
}
